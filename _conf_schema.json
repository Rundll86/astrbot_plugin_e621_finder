{
    "user_agent": {
        "description": "User-Agent",
        "hint": "UA字符串，如果出现频繁API错误就填一个，留空保持默认。",
        "type": "string",
        "default": ""
    },
    "base_url": {
        "description": "Base URL",
        "hint": "接口的基础URL地址。",
        "type": "string",
        "default": "https://e621.net"
    },
    "tag_separator": {
        "description": "关键词分隔符",
        "hint": "用户输入标签时使用此字符进行分割，用于解析请求API的参数。",
        "type": "string",
        "default": ","
    },
    "post_template": {
        "description": "帖子格式化模板",
        "hint": "把帖子数据格式化成用户方便看的形式，可以使用占位符{classpath}，只需要是e621 OpenAPI规范内允许的键即可，额外提供一个{RATING}键，代表映射过的人类可读分级标签。",
        "type": "string",
        "default": "#{id} [❤️{score} ⭐{fav_count} 📻{comment_count}]（{RATING}）\n\n{description}"
    }
}